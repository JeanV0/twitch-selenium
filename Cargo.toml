[package]
name = "twitch-selenium"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
thirtyfour = "0.31.0"
dotenv = "0.15.0"
tokio = { version = "1", features = ["full"] }
regex = "1.5"
